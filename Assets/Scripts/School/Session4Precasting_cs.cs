using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Session4Precasting_cs : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		//ray casting steps

        //raycast is using to control propority of sth infront of the camera(which means when the camera focus on sth, the thing will show property or 

        //1.create rat
	    Vector3 direction = transform.forward;
        //2.see hat ob is in front of me
        //RaycastHit


        //3. do sth with obj in front of me 
	    //if (Physics.Raycast(transform.position, castedRayDirection, out objectInFront))
                           //to take the postion      and   output recast
                           


    }
}
